const primeiroNumero = 15;
const segundoNumero = 20;

if (primeiroNumero > segundoNumero) {
  console.log(
    `Entre o ${primeiroNumero} e ${segundoNumero} o maior é ${primeiroNumero}`
  );
} else if (segundoNumero > primeiroNumero) {
  console.log(
    `Entre o ${segundoNumero} e ${primeiroNumero} o maior é ${segundoNumero}`
  );
}
