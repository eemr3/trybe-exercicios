// MongoDB Playground
// Use Ctrl+Space inside a snippet or a string literal to trigger completions.

const database = 'first_mongo_db';
const collection = 'bios';

// Create a new database.
use(database);

db.bios.find({_id: 8},{name: true});